---
phase: 06-packaging-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - packaging/spanglish-dictation.spec
  - scripts/build_portable.ps1
  - scripts/smoke_offline.ps1
  - tests/test_release_packaging.py
autonomous: true
requirements: [REL-01, REL-02]
must_haves:
  truths:
    - "A Windows user can unpack the portable zip and run the app without admin rights."
    - "The default portable artifact excludes models, CUDA DLLs, and cuDNN DLLs."
    - "The release smoke check proves privacy/network guard tests still pass."
  artifacts:
    - path: "packaging/spanglish-dictation.spec"
      provides: "PyInstaller onedir packaging contract"
      contains: "data/default_glossary.json"
    - path: "scripts/build_portable.ps1"
      provides: "Portable zip build script"
      contains: "spanglish-dictation-portable"
    - path: "tests/test_release_packaging.py"
      provides: "Release artifact policy tests"
      contains: "test_blocked_binary_patterns_are_rejected"
  key_links:
    - from: "scripts/build_portable.ps1"
      to: "packaging/spanglish-dictation.spec"
      via: "pyinstaller invocation"
      pattern: "pyinstaller.*spanglish-dictation.spec"
    - from: "scripts/smoke_offline.ps1"
      to: "tests/test_privacy_guard.py"
      via: "pytest privacy regression"
      pattern: "test_privacy_guard.py"
---

<objective>
Create the portable Windows packaging foundation and offline smoke checks.

Purpose: REL-01 requires a GitHub-legal, user-mode release path that works without cloud services or paid APIs; REL-02 requires release artifacts that are inspectable for notices and SBOM attachment.
Output: PyInstaller spec, portable zip build script, smoke script, and release policy tests.
</objective>

<execution_context>
@C:/Users/esteb/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/esteb/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-packaging-release/06-RESEARCH.md
@.planning/architecture/PRIVACY.md
@.planning/architecture/LICENSE-MATRIX.md
@pyproject.toml
@requirements.txt
@src/main.py
@data/default_glossary.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define portable packaging contract and policy tests</name>
  <files>pyproject.toml, packaging/spanglish-dictation.spec, tests/test_release_packaging.py</files>
  <read_first>pyproject.toml; requirements.txt; src/main.py; data/default_glossary.json; .planning/architecture/LICENSE-MATRIX.md</read_first>
  <behavior>
    - Test 1: `is_blocked_release_path("models/ggml-base.bin")` returns True.
    - Test 2: `is_blocked_release_path("runtime/cudnn64_9.dll")` returns True.
    - Test 3: `is_blocked_release_path("data/default_glossary.json")` returns False.
    - Test 4: `packaging/spanglish-dictation.spec` contains `src/main.py` and `data/default_glossary.json`.
  </behavior>
  <action>Add release tooling metadata without changing runtime behavior. In `pyproject.toml`, add optional dependency group `release = ["pyinstaller>=6.0", "cyclonedx-bom>=5.0"]`. Create `packaging/spanglish-dictation.spec` for PyInstaller onedir packaging with entry script `src/main.py`, app name `spanglish-dictation`, and data asset mapping `data/default_glossary.json` into `data/default_glossary.json`. Create `tests/test_release_packaging.py` with a pure helper `is_blocked_release_path(path: str) -> bool` in the test file that rejects `models/`, `*.bin`, `*.gguf`, `cudnn*.dll`, `cublas*.dll`, and `cudart*.dll`; tests must assert allowed docs/data paths are not rejected. This implements REL-01/REL-02 and the research rule that default artifacts exclude model and CUDA/cuDNN binaries.</action>
  <verify>
    <automated>python -m pytest tests/test_release_packaging.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `release = [` and `pyinstaller&gt;=6.0`.
    - `packaging/spanglish-dictation.spec` contains `src/main.py`.
    - `packaging/spanglish-dictation.spec` contains `data/default_glossary.json`.
    - `tests/test_release_packaging.py` contains `test_blocked_binary_patterns_are_rejected`.
  </acceptance_criteria>
  <done>Release packaging contract exists and tests reject blocked binary/model artifact paths.</done>
</task>

<task type="auto">
  <name>Task 2: Create portable build and offline smoke scripts</name>
  <files>scripts/build_portable.ps1, scripts/smoke_offline.ps1, tests/test_release_packaging.py</files>
  <read_first>packaging/spanglish-dictation.spec; tests/test_release_packaging.py; tests/test_privacy_guard.py; .gitignore</read_first>
  <action>Create `scripts/build_portable.ps1` with parameters `[switch]$SkipBuild` and `[string]$Version = "0.1.0"`. The script must: create `dist/release/`; when not `-SkipBuild`, run `python -m PyInstaller packaging/spanglish-dictation.spec --noconfirm`; stage `dist/spanglish-dictation/` into `dist/release/spanglish-dictation-portable-$Version/`; fail if staged files match `models\`, `*.bin`, `*.gguf`, `cudnn*.dll`, `cublas*.dll`, or `cudart*.dll`; copy `README.md`, `LICENSE`, `LICENSES/`, and `dist/release/sbom.cdx.json` when present; create `dist/release/spanglish-dictation-portable-$Version.zip`. Create `scripts/smoke_offline.ps1` that runs `python -m pytest tests/test_privacy_guard.py tests/test_release_packaging.py -q` and, when `dist/release` exists, calls `python scripts/verify_release_artifacts.py dist/release` only if that verifier exists. Extend `tests/test_release_packaging.py` to assert both scripts contain the blocked patterns and privacy pytest command.</action>
  <verify>
    <automated>python -m pytest tests/test_release_packaging.py tests/test_privacy_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/build_portable.ps1` contains `spanglish-dictation-portable-$Version.zip`.
    - `scripts/build_portable.ps1` contains `cudnn*.dll`, `cublas*.dll`, and `cudart*.dll`.
    - `scripts/smoke_offline.ps1` contains `tests/test_privacy_guard.py`.
    - `tests/test_release_packaging.py` contains `test_build_script_blocks_forbidden_artifacts`.
  </acceptance_criteria>
  <done>Portable build and offline smoke scripts are present and enforce blocked artifact policy before zip creation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build machine → release zip | Local files become user-distributed artifacts. |
| runtime app → OS/network APIs | App execution must not cross into network activity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `scripts/build_portable.ps1` | mitigate | Fail build if `models/`, `*.bin`, `*.gguf`, CUDA, or cuDNN DLLs appear in staged artifact. |
| T-06-02 | Tampering | portable zip | mitigate | Later checksum plan signs off SHA-256; this plan creates deterministic staging folder naming. |
| T-06-03 | Elevation of Privilege | installer path | accept | Portable zip performs no installer elevation or admin actions. |
| T-06-04 | Information Disclosure | runtime smoke path | mitigate | `scripts/smoke_offline.ps1` runs privacy guard tests before release. |
</threat_model>

<verification>
Run `python -m pytest tests/test_release_packaging.py tests/test_privacy_guard.py -q`.
</verification>

<success_criteria>
- Portable packaging files exist.
- Blocked artifacts are rejected by automated tests and build script checks.
- Offline smoke script includes privacy regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/06-packaging-release/06-01-SUMMARY.md`.
</output>
