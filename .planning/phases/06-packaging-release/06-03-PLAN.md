---
phase: 06-packaging-release
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - README.md
  - LICENSE
  - docs/RELEASE.md
  - docs/MODEL-SIDELOADING.md
  - docs/PRIVACY.md
  - tests/test_release_docs.py
autonomous: true
requirements: [REL-01, REL-02]
must_haves:
  truths:
    - "A GitHub visitor understands the project is open source and fully offline at runtime."
    - "A user can side-load model files using documented checksums without runtime downloads."
    - "Release docs explain portable zip usage, SBOM, license notices, and privacy constraints."
  artifacts:
    - path: "README.md"
      provides: "Repository landing page"
      contains: "fully offline"
    - path: "LICENSE"
      provides: "MIT project license text"
      contains: "MIT License"
    - path: "docs/MODEL-SIDELOADING.md"
      provides: "Model side-loading instructions"
      contains: "ggml-base.bin"
  key_links:
    - from: "README.md"
      to: "docs/MODEL-SIDELOADING.md"
      via: "model documentation link"
      pattern: "MODEL-SIDELOADING.md"
    - from: "docs/RELEASE.md"
      to: "LICENSES/THIRD-PARTY-NOTICES.md"
      via: "release checklist reference"
      pattern: "THIRD-PARTY-NOTICES.md"
---

<objective>
Write release documentation, public README, privacy statement, and app license.

Purpose: REL-01 needs an open-source GitHub-ready repository; REL-02 release artifacts need discoverable notice, SBOM, side-loading, and privacy documentation.
Output: README, LICENSE, release guide, model side-loading guide, privacy doc, and documentation tests.
</objective>

<execution_context>
@C:/Users/esteb/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/esteb/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-packaging-release/06-RESEARCH.md
@.planning/architecture/PRIVACY.md
@.planning/architecture/LICENSE-MATRIX.md
@models/MODEL-REGISTRY.md
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add release documentation tests</name>
  <files>tests/test_release_docs.py</files>
  <read_first>.planning/architecture/PRIVACY.md; .planning/architecture/LICENSE-MATRIX.md; models/MODEL-REGISTRY.md; pyproject.toml</read_first>
  <behavior>
    - Test 1: `README.md` contains `fully offline`, `No telemetry`, and `docs/MODEL-SIDELOADING.md`.
    - Test 2: `LICENSE` contains `MIT License`.
    - Test 3: `docs/RELEASE.md` contains `SBOM`, `LICENSES/`, and `SHA-256`.
    - Test 4: `docs/MODEL-SIDELOADING.md` contains both side-loaded model filenames and checksums.
    - Test 5: `docs/PRIVACY.md` contains `no runtime network`, `no telemetry`, `no retained audio`, and `no retained transcripts`.
  </behavior>
  <action>Create `tests/test_release_docs.py` with pathlib-based tests that read the exact files listed above and assert required strings. Use case-insensitive checks only for privacy phrases; use exact checks for filenames and hashes. Include the two hashes from `models/MODEL-REGISTRY.md`: `60ed5bc3dd14eea856493d334349b405782ddcaf0028d4b5df4088345fba2efe` and `1be3a9b2063867b937e64e2ec7483364a79917e157fa98c5d94b5c1fffea987b`.</action>
  <verify>
    <automated>python -m pytest tests/test_release_docs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_release_docs.py` contains `test_readme_states_offline_privacy_and_side_loading`.
    - `tests/test_release_docs.py` contains both model SHA-256 hashes.
  </acceptance_criteria>
  <done>Documentation expectations are encoded before writing release docs.</done>
</task>

<task type="auto">
  <name>Task 2: Write GitHub-ready release docs and MIT license</name>
  <files>README.md, LICENSE, docs/RELEASE.md, docs/MODEL-SIDELOADING.md, docs/PRIVACY.md</files>
  <read_first>tests/test_release_docs.py; .planning/architecture/PRIVACY.md; .planning/architecture/LICENSE-MATRIX.md; models/MODEL-REGISTRY.md; requirements.txt</read_first>
  <action>Create `README.md` for Windows Offline Spanglish Dictation with sections: `## What it does`, `## Privacy guarantees`, `## Install from portable zip`, `## Model side-loading`, `## Development`, and `## Release artifacts`. State these exact phrases: `fully offline`, `No telemetry`, `No runtime model downloads`, and link to `docs/MODEL-SIDELOADING.md`. Create `LICENSE` with standard MIT License text and copyright holder `Spanglish Dictation Team`. Create `docs/RELEASE.md` with a release checklist covering portable zip, `dist/release/sbom.cdx.json`, `LICENSES/`, SHA-256 checksums, offline smoke command `powershell -ExecutionPolicy Bypass -File scripts/smoke_offline.ps1`, and the blocked artifact list. Create `docs/MODEL-SIDELOADING.md` with `models/` directory instructions, `ggml-base.bin` and `ggml-small.bin` filenames, sizes, hashes, source URL, and explicit statement that the app never downloads models at runtime. Create `docs/PRIVACY.md` as public release privacy statement with exact guarantees from `.planning/architecture/PRIVACY.md`: no runtime network, no telemetry, no cloud fallback, no retained audio, no retained transcripts by default, redacted local-only diagnostics.</action>
  <verify>
    <automated>python -m pytest tests/test_release_docs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `No runtime model downloads`.
    - `LICENSE` contains `MIT License`.
    - `docs/RELEASE.md` contains `powershell -ExecutionPolicy Bypass -File scripts/smoke_offline.ps1`.
    - `docs/MODEL-SIDELOADING.md` contains both model SHA-256 hashes.
    - `docs/PRIVACY.md` contains `no retained transcripts by default`.
  </acceptance_criteria>
  <done>Repository has public release, model, privacy, and license documentation that passes automated string checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository docs → user behavior | Public docs guide installation, model side-loading, and privacy expectations. |
| local model URLs → user downloads | URLs are documentation metadata, not runtime fetch targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Spoofing | model side-loading docs | mitigate | Publish SHA-256 checksums and source URL for each model. |
| T-06-09 | Repudiation | privacy docs | mitigate | Public docs repeat exact privacy guarantees from architecture. |
| T-06-10 | Information Disclosure | README/install docs | mitigate | State no telemetry, no runtime network, no retained audio/transcripts. |
</threat_model>

<verification>
Run `python -m pytest tests/test_release_docs.py -q`.
</verification>

<success_criteria>
- README and LICENSE make repo GitHub-ready.
- Release docs explain portable zip, SBOM, notices, checksums, and blocked artifacts.
- Model side-loading docs contain hashes and no-runtime-download statement.
</success_criteria>

<output>
After completion, create `.planning/phases/06-packaging-release/06-03-SUMMARY.md`.
</output>
