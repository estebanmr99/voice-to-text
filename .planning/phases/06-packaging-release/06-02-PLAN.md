---
phase: 06-packaging-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_license_bundle.py
  - LICENSES/THIRD-PARTY-NOTICES.md
  - LICENSES/MODEL-NOTICES.md
  - tests/test_license_bundle.py
autonomous: true
requirements: [REL-02]
must_haves:
  truths:
    - "Release artifacts include SBOM generation instructions and license notices."
    - "Every runtime dependency has a conservative notice entry."
    - "Model assets are documented by metadata and checksums, not bundled binaries."
  artifacts:
    - path: "scripts/generate_license_bundle.py"
      provides: "License notice and SBOM helper"
      contains: "cyclonedx-py"
    - path: "LICENSES/THIRD-PARTY-NOTICES.md"
      provides: "Runtime dependency notices"
      contains: "PySide6"
    - path: "LICENSES/MODEL-NOTICES.md"
      provides: "Model asset license/checksum notes"
      contains: "ggml-base.bin"
  key_links:
    - from: "scripts/generate_license_bundle.py"
      to: "requirements.txt"
      via: "dependency list parsing"
      pattern: "requirements.txt"
    - from: "LICENSES/MODEL-NOTICES.md"
      to: "models/MODEL-REGISTRY.md"
      via: "copied model metadata"
      pattern: "60ed5bc3dd14eea856493d334349b405782ddcaf0028d4b5df4088345fba2efe"
---

<objective>
Generate conservative release license notices and SBOM support.

Purpose: REL-02 requires SBOM and license notices for runtime dependencies and model assets before release.
Output: License notice files, SBOM helper script, and tests that enforce notice coverage.
</objective>

<execution_context>
@C:/Users/esteb/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/esteb/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-packaging-release/06-RESEARCH.md
@.planning/architecture/LICENSE-MATRIX.md
@models/MODEL-REGISTRY.md
@requirements.txt
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create license bundle generator and coverage tests</name>
  <files>scripts/generate_license_bundle.py, tests/test_license_bundle.py</files>
  <read_first>requirements.txt; pyproject.toml; .planning/architecture/LICENSE-MATRIX.md; models/MODEL-REGISTRY.md</read_first>
  <behavior>
    - Test 1: notices include PySide6, sounddevice, numpy, pywhispercpp, pywin32.
    - Test 2: notices include WebRTC VAD as optional runtime VAD notice.
    - Test 3: notices include blocked CUDA/cuDNN redistribution language.
    - Test 4: `--check` exits 0 when generated notice files contain all required tokens.
  </behavior>
  <action>Create `scripts/generate_license_bundle.py` with constants for required notice tokens: `PySide6`, `Qt`, `sounddevice`, `PortAudio`, `numpy`, `pywhispercpp`, `whisper.cpp`, `pywin32`, `WebRTC VAD`, `Whisper model weights`, `faster-whisper`, `CTranslate2`, `CUDA/cuDNN redistribution blocked`. Implement CLI flags `--write` and `--check`; `--write` creates `LICENSES/THIRD-PARTY-NOTICES.md` and `LICENSES/MODEL-NOTICES.md`; `--check` verifies those files exist and contain every required token. Include a printed SBOM command exactly: `python -m cyclonedx_py requirements -i requirements.txt -o dist/release/sbom.cdx.json`. Create `tests/test_license_bundle.py` that imports the script module and verifies required tokens and check behavior using temp files.</action>
  <verify>
    <automated>python -m pytest tests/test_license_bundle.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/generate_license_bundle.py` contains `REQUIRED_NOTICE_TOKENS`.
    - `scripts/generate_license_bundle.py` contains `cyclonedx_py requirements`.
    - `tests/test_license_bundle.py` contains `test_required_notice_tokens_cover_runtime_dependencies`.
  </acceptance_criteria>
  <done>License bundle generator exists with automated token coverage tests.</done>
</task>

<task type="auto">
  <name>Task 2: Write conservative notices for dependencies and models</name>
  <files>LICENSES/THIRD-PARTY-NOTICES.md, LICENSES/MODEL-NOTICES.md, scripts/generate_license_bundle.py</files>
  <read_first>scripts/generate_license_bundle.py; .planning/architecture/LICENSE-MATRIX.md; models/MODEL-REGISTRY.md; requirements.txt</read_first>
  <action>Run or mirror `python scripts/generate_license_bundle.py --write` to create the notice files. `LICENSES/THIRD-PARTY-NOTICES.md` must include sections for PySide6/Qt, sounddevice/PortAudio, numpy, pywhispercpp/whisper.cpp, pywin32, optional WebRTC VAD, optional faster-whisper/CTranslate2, and a CUDA/cuDNN blocked redistribution section. `LICENSES/MODEL-NOTICES.md` must include `ggml-base.bin`, `ggml-small.bin`, both SHA-256 values from `models/MODEL-REGISTRY.md`, source URL `https://huggingface.co/ggerganov/whisper.cpp/tree/main`, and the statement `Model binaries are not included in git or the default portable zip`.</action>
  <verify>
    <automated>python scripts/generate_license_bundle.py --check</automated>
  </verify>
  <acceptance_criteria>
    - `LICENSES/THIRD-PARTY-NOTICES.md` contains `CUDA/cuDNN redistribution blocked`.
    - `LICENSES/THIRD-PARTY-NOTICES.md` contains `PySide6` and `PortAudio`.
    - `LICENSES/MODEL-NOTICES.md` contains `ggml-base.bin` and `ggml-small.bin`.
    - `LICENSES/MODEL-NOTICES.md` contains `Model binaries are not included in git or the default portable zip`.
  </acceptance_criteria>
  <done>Notice bundle covers runtime dependencies, optional dev dependencies, blocked GPU redistribution, and model metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dependency metadata → release notices | Third-party package names and licenses become public legal-facing docs. |
| local model registry → release notices | Local model hashes become documentation without bundling binaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Repudiation | license notices | mitigate | `--check` fails when required dependency tokens are absent. |
| T-06-06 | Information Disclosure | model notices | mitigate | Notices publish hashes/source URL only; no model binaries copied. |
| T-06-07 | Tampering | SBOM path | mitigate | SBOM output path fixed to `dist/release/sbom.cdx.json`. |
</threat_model>

<verification>
Run `python -m pytest tests/test_license_bundle.py -q` and `python scripts/generate_license_bundle.py --check`.
</verification>

<success_criteria>
- `LICENSES/` covers all runtime dependencies and model metadata.
- SBOM command is scripted for release output.
- CUDA/cuDNN remains explicitly blocked.
</success_criteria>

<output>
After completion, create `.planning/phases/06-packaging-release/06-02-SUMMARY.md`.
</output>
