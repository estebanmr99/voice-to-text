---
phase: 02-mvp-offline-dictation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - requirements.txt
  - src/__init__.py
  - src/main.py
  - src/settings_store.py
  - src/diagnostics.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_settings_store.py
  - tests/test_diagnostics.py
autonomous: true
requirements:
  - CORE-09
must_haves:
  truths:
    - "Project has standard Python package structure with src/ layout"
    - "PySide6 application launches without errors and shows system tray icon"
    - "Settings are persisted to a local JSON file and survive app restart"
    - "No admin privileges are required to run the application"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata and build configuration"
      min_lines: 20
    - path: "src/settings_store.py"
      provides: "Local preferences persistence"
      exports: ["SettingsStore"]
    - path: "src/main.py"
      provides: "Application entry point with PySide6 QApplication"
      exports: ["main"]
    - path: "src/diagnostics.py"
      provides: "Redacted local-only event logging"
      exports: ["Diagnostics"]
  key_links:
    - from: "src/main.py"
      to: "src/settings_store.py"
      via: "import"
      pattern: "from settings_store import SettingsStore"
    - from: "src/main.py"
      to: "src/diagnostics.py"
      via: "import"
      pattern: "from diagnostics import Diagnostics"
---

<objective>
Scaffold the Python project with src/ layout, dependency declarations, PySide6 shell with system tray icon, SettingsStore for local preferences, and Diagnostics for redacted local-only events.

Purpose: Establish the foundation that all other MVP modules build upon.
Output: Runnable PySide6 app with tray icon, persistent settings, and diagnostic logging.
</objective>

<execution_context>
@C:/Users/esteb/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-mvp-offline-dictation/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project structure and dependency files</name>
  <files>pyproject.toml, requirements.txt, src/__init__.py, tests/__init__.py, tests/conftest.py</files>
  <action>
Create standard Python src-layout project files:

1. `pyproject.toml`:
   - build-system: setuptools
   - project name: "spanglish-dictation"
   - python_requires: ">=3.11"
   - dependencies: PySide6>=6.5, sounddevice>=0.5, numpy>=1.24, pywhispercpp>=1.4
   - optional-dependencies dev: pytest>=8.0, black, ruff, mypy

2. `requirements.txt`:
   - Pin exact versions for reproducible installs
   - PySide6==6.8.0 (or latest stable)
   - sounddevice==0.5.5
   - numpy==1.26.4
   - pywhispercpp==1.4.1
   - webrtcvad-wheels==2.0.11 (pre-built wheel alternative to webrtcvad)
   - pywin32==306
   - Include comment: "# faster-whisper and ctranslate2 are dev-only; do not ship"

3. `src/__init__.py`: empty (or with __version__)
4. `tests/__init__.py`: empty
5. `tests/conftest.py`: pytest fixtures for QApplication singleton (use pytest-qt or manual fixture)

Do NOT create any model files or binary assets.
  </action>
  <verify>
    <automated>python -c "import tomllib; f=open('pyproject.toml','rb'); d=tomllib.load(f); assert d['project']['name']=='spanglish-dictation'; assert 'PySide6' in str(d['project']['dependencies'])"</automated>
    <automated>grep -c "pywhispercpp" requirements.txt</automated>
    <automated>test -d src && test -d tests</automated>
  </verify>
  <done>
    - pyproject.toml exists with correct project metadata and dependencies
    - requirements.txt exists with pinned versions
    - src/ and tests/ directories exist with __init__.py files
    - tests/conftest.py exists with QApplication fixture
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement SettingsStore with JSON persistence</name>
  <files>src/settings_store.py, tests/test_settings_store.py</files>
  <action>
Implement `src/settings_store.py`:

Class `SettingsStore`:
- `__init__(self, path: Path | None = None)` — default to `Path.home() / ".spanglish-dictation" / "settings.json"`
- `_ensure_dir()` — create parent dirs with exist_ok=True
- `get(self, key: str, default=None)` — read from in-memory dict
- `set(self, key: str, value)` — write to in-memory dict and persist
- `load()` — read JSON from disk, populate dict
- `save()` — write JSON to disk with indent=2
- Properties for known keys with defaults:
  - `hotkey_push_to_talk: str = "Ctrl+Alt+D"`
  - `hotkey_toggle: str = "Ctrl+Alt+T"`
  - `audio_device_index: int | None = None` (None = default)
  - `vad_profile: str = "webrtc"` (options: "webrtc", "silero")
  - `model_profile: str = "cpu-portable"`
  - `paste_mode: str = "immediate"` (options: "immediate", "confirmation")
  - `language: str = "auto"`

Use `@dataclass` for typed settings if feasible, or plain dict with validation.
File must be importable without PySide6 (no GUI deps in settings module).

`tests/test_settings_store.py`:
- Test default values
- Test persistence round-trip (set → save → new instance → load → get)
- Test custom path
- Use tmp_path fixture for isolation
  </action>
  <verify>
    <automated>pytest tests/test_settings_store.py -x -q</automated>
    <automated>grep -c "class SettingsStore" src/settings_store.py</automated>
    <automated>grep -c "def load(" src/settings_store.py</automated>
    <automated>grep -c "def save(" src/settings_store.py</automated>
  </verify>
  <done>
    - SettingsStore class exists with get/set/load/save methods
    - Default values match specification
    - JSON file persists to disk and reloads correctly
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Diagnostics and PySide6 tray shell</name>
  <files>src/diagnostics.py, src/main.py, tests/test_diagnostics.py</files>
  <action>
Implement `src/diagnostics.py`:

Class `Diagnostics`:
- `__init__(self, log_dir: Path | None = None)` — default to `Path.home() / ".spanglish-dictation" / "logs"`
- `event(self, name: str, **kwargs)` — log structured event with timestamp, event name, and REDACTED kwargs (only log key names, never values that could contain transcript content or PII)
- `_rotate_if_needed()` — keep max 10 log files, 1MB each
- `get_log_path() -> Path`
- Events are written to `app-YYYY-MM-DD.log` as JSON lines
- Example event: `{"t":"2026-05-04T12:00:00","e":"dictation_started","keys":["audio_device"]}` — NEVER include the actual audio device name or transcript text

Implement `src/main.py`:

Function `main()`:
1. Create `QApplication(sys.argv)`
2. Call `app.setQuitOnLastWindowClosed(False)` — tray-only app
3. Create `QSystemTrayIcon` with a default icon (use `QIcon.fromTheme("audio-input-microphone")` or a bundled placeholder PNG)
4. Create `QMenu` with actions: Start, Stop, Settings (placeholder), Exit
5. Connect `Exit` to `app.quit()`
6. Connect `activated` signal to show menu on left-click
7. Create `SettingsStore` instance
8. Create `Diagnostics` instance
9. Show tray icon with `tray_icon.show()`
10. Call `app.exec()`

`tests/test_diagnostics.py`:
- Test event logging creates file
- Test redaction (values not in log)
- Test rotation logic
- Use tmp_path for isolation
  </action>
  <verify>
    <automated>pytest tests/test_diagnostics.py -x -q</automated>
    <automated>grep -c "class Diagnostics" src/diagnostics.py</automated>
    <automated>grep -c "QSystemTrayIcon" src/main.py</automated>
    <automated>grep -c "setQuitOnLastWindowClosed(False)" src/main.py</automated>
    <automated>python -c "import sys; sys.path.insert(0,'src'); from main import main; print('import ok')"</automated>
  </verify>
  <done>
    - Diagnostics class logs redacted JSON events to dated files
    - main.py launches QApplication with system tray icon
    - Tray menu has Start, Stop, Settings, Exit actions
    - App runs without admin and stays in tray
    - All tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Settings File | Local filesystem, trusted user account |
| Diagnostics → Log File | Local filesystem, must never contain PII/transcripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | diagnostics.py | mitigate | Redact all values; only log key names and event types. Audit: grep log files for transcript content must return 0 matches. |
| T-02-02 | Tampering | settings.json | accept | Local file in user home; no sensitive data stored. Integrity not critical for MVP. |
| T-02-03 | Denial of Service | settings_store.py | mitigate | JSON parsing with try/except; fallback to defaults on corruption. |
</threat_model>

<verification>
- `python -m pytest tests/ -x -q` passes
- `python src/main.py` launches and shows tray icon (manual check)
- `grep -r "transcript" logs/` returns no matches in diagnostics logs
- Settings file created in `~/.spanglish-dictation/settings.json`
</verification>

<success_criteria>
- Project installs with `pip install -r requirements.txt` without errors
- `python src/main.py` shows Windows system tray icon
- Settings persist across app restarts
- Diagnostics logs contain only redacted event metadata
- No admin prompt appears on launch
</success_criteria>

<output>
After completion, create `.planning/phases/02-mvp-offline-dictation/02-01-SUMMARY.md`
</output>
