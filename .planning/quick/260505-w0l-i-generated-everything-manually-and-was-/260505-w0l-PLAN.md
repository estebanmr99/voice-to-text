---
phase: quick-260505-w0l-release-publication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/releases/v0.1.0.md
  - README.md
  - docs/RELEASE.md
  - docs/GITHUB-RELEASE-CHECKLIST.md
  - tests/test_release_docs.py
  - dist/release/spanglish-dictation-portable-0.1.0.zip
  - dist/release/sbom.cdx.json
  - dist/release/SHA256SUMS.txt
  - .planning/STATE.md
  - .planning/quick/260505-w0l-i-generated-everything-manually-and-was-/260505-w0l-SUMMARY.md
autonomous: true
requirements: [REL-01, REL-02]
must_haves:
  truths:
    - "A public GitHub release for v0.1.0 exists and points users to the offline Windows portable build."
    - "The published release assets include the portable zip, SBOM, and checksums, with no bundled models or blocked CUDA/cuDNN DLLs."
    - "The release page explains offline/privacy guarantees and side-loading expectations instead of relying on autogenerated notes alone."
  artifacts:
    - path: "docs/releases/v0.1.0.md"
      provides: "Curated release notes used for the GitHub release body"
    - path: "dist/release/spanglish-dictation-portable-0.1.0.zip"
      provides: "Portable Windows release artifact"
    - path: "dist/release/sbom.cdx.json"
      provides: "CycloneDX SBOM for the published release"
    - path: "dist/release/SHA256SUMS.txt"
      provides: "Published integrity hashes for release assets"
  key_links:
    - from: "docs/releases/v0.1.0.md"
      to: "GitHub release v0.1.0"
      via: "gh release edit --notes-file"
      pattern: "v0.1.0"
    - from: "scripts/prepare_release.ps1"
      to: "dist/release/"
      via: "release pipeline"
      pattern: "prepare_release"
    - from: "dist/release/SHA256SUMS.txt"
      to: "dist/release/spanglish-dictation-portable-0.1.0.zip"
      via: "checksum verification"
      pattern: "SHA256"
---

<objective>
Publish the first public GitHub release for this app using the already-built Phase 6 packaging pipeline, then record the release state so the repo and planning artifacts both show a real shipped v0.1.0.

Purpose: finish the project's release work end-to-end instead of stopping at local bundle generation.
Output: curated v0.1.0 release notes, verified release artifacts, a live GitHub release, and updated planning state.
</objective>

<execution_context>
@./AGENTS.md
@.planning/STATE.md
</execution_context>

<context>
@README.md
@docs/RELEASE.md
@docs/GITHUB-RELEASE-CHECKLIST.md
@.planning/phases/06-packaging-release/06-VALIDATION.md
@.planning/phases/06-packaging-release/06-04-SUMMARY.md
@.planning/quick/260505-v8d-ensure-readme-install-docs-are-complete-/260505-v8d-SUMMARY.md

Current repo facts already established:
- The GitHub repo already exists at `https://github.com/estebanmr99/voice-to-text`.
- `dist/release/` already contains a local `0.1.0` portable zip, SBOM, and checksums from the prior quick task.
- The remaining gap is publication: a tagged GitHub release with curated notes and verified attached assets.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create canonical v0.1.0 release notes and lock doc references</name>
  <files>docs/releases/v0.1.0.md, README.md, docs/RELEASE.md, docs/GITHUB-RELEASE-CHECKLIST.md, tests/test_release_docs.py</files>
  <behavior>
    - Test 1: release docs reference the real GitHub Releases page and the exact `v0.1.0` publish flow.
    - Test 2: `docs/releases/v0.1.0.md` contains user-facing highlights, offline/privacy guarantees, side-loading instructions, and attached-asset expectations.
    - Test 3: automated doc checks fail if the canonical release-notes file or required release links disappear.
  </behavior>
  <action>Create `docs/releases/v0.1.0.md` as the canonical release body for the first public ship. Include: what the app does, the offline/privacy guarantees, the exact portable artifact name, the requirement to side-load models, and links back to `docs/INSTALL.md`, `docs/PRIVACY.md`, and `docs/MODEL-SIDELOADING.md`. Update `README.md`, `docs/RELEASE.md`, and `docs/GITHUB-RELEASE-CHECKLIST.md` only where needed so they consistently describe publishing `v0.1.0` and point to the live GitHub Releases page. Extend `tests/test_release_docs.py` so the release-notes file and required public links are enforced by automation instead of manual checking.</action>
  <verify>
    <automated>python -m pytest tests/test_release_docs.py -q</automated>
  </verify>
  <done>`docs/releases/v0.1.0.md` exists, release docs are version-consistent, and doc tests prove the public release contract is present.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild, verify, and publish the v0.1.0 GitHub release</name>
  <files>dist/release/spanglish-dictation-portable-0.1.0.zip, dist/release/sbom.cdx.json, dist/release/SHA256SUMS.txt</files>
  <action>From a clean working tree, rerun `scripts/prepare_release.ps1 -Version 0.1.0` so the release artifacts are freshly regenerated under the same blocked-artifact policy used in Phase 6. Do not hand-edit `dist/release/` contents and do not bypass the verifier. If `v0.1.0` is not already tagged, create an annotated tag from the release commit, push the branch and tag, wait for the tag-triggered GitHub Actions release workflow to finish, then use GitHub CLI to ensure the release exists, is published (not draft), has the expected assets attached, and has its body replaced with `docs/releases/v0.1.0.md` rather than generic autogenerated notes. If the workflow already created the release, edit it in place instead of creating a duplicate.</action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/prepare_release.ps1 -Version 0.1.0; if ($?) { gh release view v0.1.0 --json name,isDraft,isPrerelease,url,assets }</automated>
  </verify>
  <done>The public `v0.1.0` GitHub release exists, is not a draft/prerelease unless explicitly intended, and exposes the verified portable zip, SBOM, and checksums.</done>
</task>

<task type="auto">
  <name>Task 3: Record the shipped release in planning state</name>
  <files>.planning/STATE.md, .planning/quick/260505-w0l-i-generated-everything-manually-and-was-/260505-w0l-SUMMARY.md</files>
  <action>Update `.planning/STATE.md` so the current position, recent decisions, blockers, and next action reflect that the first public release was shipped. Note the release tag, GitHub release URL, and whether any follow-up work remains for post-release verification only. Then create `.planning/quick/260505-w0l-i-generated-everything-manually-and-was-/260505-w0l-SUMMARY.md` summarizing the exact commands run, the assets published, the release URL, and any post-release observations. Keep this strictly factual; do not reopen scope or add a new planning phase here.</action>
  <verify>
    <automated>gh release view v0.1.0 --json url,assets; if ($?) { python -c "from pathlib import Path; assert Path(r'.planning/quick/260505-w0l-i-generated-everything-manually-and-was-/260505-w0l-SUMMARY.md').exists(); assert Path(r'.planning/STATE.md').exists(); print('planning state updated')" }</automated>
  </verify>
  <done>The planning artifacts clearly show that `v0.1.0` shipped and point to the real GitHub release for future resumes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local release artifacts → public GitHub release | Shipping the wrong files can leak blocked binaries or incomplete notices |
| release notes/docs → end users | Inaccurate notes can mislead users about offline/privacy or model setup |
| git tag/workflow → GitHub release page | Wrong tag/release handling can publish duplicate or stale assets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-w0l-01 | I | `dist/release/` | mitigate | Re-run `scripts/prepare_release.ps1` and keep `scripts/verify_release_artifacts.py` as the hard gate before publication |
| T-quick-w0l-02 | T | GitHub release body/assets | mitigate | Use `gh release view/edit` against `v0.1.0` only and replace autogenerated notes with `docs/releases/v0.1.0.md` |
| T-quick-w0l-03 | R | public release guidance | mitigate | Keep release notes explicit about offline runtime, no bundled models, and side-loading docs |
| T-quick-w0l-04 | D | release pipeline state | mitigate | Record the final release URL and shipped tag in `.planning/STATE.md` and the quick-task summary for future resume clarity |
</threat_model>

<verification>
- `python -m pytest tests/test_release_docs.py -q`
- `powershell -ExecutionPolicy Bypass -File scripts/prepare_release.ps1 -Version 0.1.0`
- `gh release view v0.1.0 --json name,isDraft,isPrerelease,url,assets`
</verification>

<success_criteria>
- `docs/releases/v0.1.0.md` exists and becomes the live GitHub release body.
- GitHub shows a real `v0.1.0` release for `estebanmr99/voice-to-text` with the portable zip, SBOM, and checksums attached.
- The published release and docs preserve the project's no-runtime-network, no-telemetry, no-bundled-models constraints.
- `.planning/STATE.md` and the quick summary record the shipped release so the next session resumes from post-release work instead of unpublished packaging.
</success_criteria>

<output>
After completion, create `.planning/quick/260505-w0l-i-generated-everything-manually-and-was-/260505-w0l-SUMMARY.md`.
</output>
