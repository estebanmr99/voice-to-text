---
phase: quick-260505-v8d-publish-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/INSTALL.md
  - docs/RELEASE.md
  - docs/GITHUB-RELEASE-CHECKLIST.md
  - .github/workflows/release.yml
  - .gitignore
  - tests/test_release_docs.py
  - tests/test_release_workflow.py
autonomous: true
requirements: [REL-01, REL-02]
must_haves:
  truths:
    - "A first-time Windows user can follow repo docs from clone/download to runnable portable app without guessing missing steps."
    - "The public repo explains why the GitHub Actions release workflow exists and it remains tag-only release automation, not runtime behavior."
    - "Public publishing excludes local planning scratch such as .sisyphus/ and ships a verified portable zip plus release metadata."
  artifacts:
    - path: "README.md"
      provides: "Public repo entrypoint with real install/release links and publish guidance"
    - path: "docs/INSTALL.md"
      provides: "Explicit Windows install + first-run instructions"
    - path: ".github/workflows/release.yml"
      provides: "Tag-triggered GitHub release automation"
    - path: "dist/release/spanglish-dictation-portable-0.1.0.zip"
      provides: "Portable release bundle"
  key_links:
    - from: "README.md"
      to: "docs/INSTALL.md"
      via: "install section link"
      pattern: "docs/INSTALL.md"
    - from: ".github/workflows/release.yml"
      to: "docs/GITHUB-RELEASE-CHECKLIST.md"
      via: "documented publish flow"
      pattern: "v\*|action-gh-release"
    - from: ".gitignore"
      to: ".sisyphus/"
      via: "public repo exclusion"
      pattern: "\.sisyphus/"
---

<objective>
Make the repository publish-ready: complete end-user install/release docs, keep only the release automation that supports GitHub publishing, exclude non-product planning scratch, then create/push the GitHub repo and generate the first portable release bundle.

Purpose: finish the last public-facing packaging gaps before publishing this Windows-first offline app.
Output: complete README/install/release docs, clarified release workflow policy, cleaned repo contents, pushed GitHub repo, and verified `dist/release/` assets.
</objective>

<execution_context>
@./AGENTS.md
@.planning/STATE.md
</execution_context>

<context>
@README.md
@docs/RELEASE.md
@docs/GITHUB-RELEASE-CHECKLIST.md
@.github/workflows/release.yml
@.gitignore

Current repo facts already in codebase:
- `README.md` still contains a placeholder GitHub Releases URL.
- There is no dedicated `docs/INSTALL.md` yet.
- `.github/workflows/release.yml` was intentionally added in Phase 6 as tag-triggered release automation.
- `.sisyphus/` contains planning evidence and scratch files; it is not part of the runtime app or release artifacts.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete the public install and release documentation</name>
  <files>README.md, docs/INSTALL.md, docs/RELEASE.md, docs/GITHUB-RELEASE-CHECKLIST.md, tests/test_release_docs.py</files>
  <behavior>
    - Test 1: `README.md` links to `docs/INSTALL.md`, `docs/RELEASE.md`, and the real GitHub Releases URL for this repo instead of `https://github.com/...`.
    - Test 2: `docs/INSTALL.md` covers Windows prerequisites, portable zip extraction, model side-loading, first launch, hotkey/start flow, and where logs/settings live.
    - Test 3: release docs describe the exact manual commands to build and verify the portable zip before publishing.
  </behavior>
  <action>Create `docs/INSTALL.md` and update the public docs so a new Windows user can install and run the app without inferring hidden steps. Replace the placeholder GitHub Releases URL in `README.md` with the real repo URL after repo creation, add explicit links to install/release/privacy/model-side-loading docs, and expand `docs/RELEASE.md` plus `docs/GITHUB-RELEASE-CHECKLIST.md` so they cover the same end-to-end flow: build portable zip, side-load models, run offline smoke checks, verify notices/SBOM/checksums, and publish assets. Extend `tests/test_release_docs.py` so these links/sections are enforced by automated content checks.</action>
  <verify>
    <automated>python -m pytest tests/test_release_docs.py -q</automated>
  </verify>
  <done>`README.md` has no placeholder release URL, `docs/INSTALL.md` exists and is linked from README, and release/install docs describe a complete first-run path for an offline Windows user.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make repo hygiene explicit for public GitHub publishing</name>
  <files>.github/workflows/release.yml, .gitignore, docs/RELEASE.md, docs/GITHUB-RELEASE-CHECKLIST.md, tests/test_release_workflow.py</files>
  <behavior>
    - Test 1: the release workflow is clearly tag-triggered (`v*`) and only exists to build/attach GitHub release assets.
    - Test 2: public docs explain that GitHub Actions network activity is CI-time only and does not violate the app's no-runtime-network guarantee.
    - Test 3: `.sisyphus/` is excluded from the public repo path unless the user explicitly chooses to publish planning artifacts.
  </behavior>
  <action>Keep `.github/workflows/release.yml` only as release automation for GitHub publishing; do not broaden it into general CI and do not let docs imply it is required for runtime operation. Update docs to explain why it exists: pushing a `v*` tag builds and uploads release assets. Treat `.sisyphus/` as local planning scratch, not a shipping or public-source requirement: add `.sisyphus/` to `.gitignore`, remove tracked `.sisyphus/` files from the publish commit if they are still tracked, and scrub any public-facing release docs that suggest `.sisyphus/` is required to install, build, or release the app. Extend `tests/test_release_workflow.py` so the workflow remains tag-only, forbidden runtime-download patterns stay absent, and the public publish checklist asserts `.sisyphus/` is excluded.</action>
  <verify>
    <automated>python -m pytest tests/test_release_workflow.py -q</automated>
  </verify>
  <done>The repo clearly distinguishes product files from planning scratch, `.sisyphus/` is not required for the public repo, and the GitHub workflow is documented as optional release automation rather than app behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Create the GitHub repo, push the code, and produce the release bundle</name>
  <files>README.md, dist/release/spanglish-dictation-portable-0.1.0.zip, dist/release/sbom.cdx.json, dist/release/SHA256SUMS.txt</files>
  <action>Using GitHub CLI, create the repository under the user's account, add the `origin` remote, push the current branch and tags, then run the existing release pipeline locally with `scripts/prepare_release.ps1 -Version 0.1.0`. After the repo exists, update `README.md` and any release docs that need the final GitHub URL, rerun the doc tests if the URL changed, and verify `dist/release/` contains the portable zip, SBOM, and checksum file. If GitHub CLI auth blocks the push, stop at the auth gate, let the user authenticate, then resume the same commands rather than changing the plan.</action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/prepare_release.ps1 -Version 0.1.0; if ($?) { gh repo view --json url,defaultBranchRef; if ($?) { git ls-remote --heads origin } }</automated>
  </verify>
  <done>A real GitHub repo exists with the code pushed, README links resolve to that repo, and `dist/release/` contains a verified portable zip, SBOM, and checksums ready for a GitHub release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → GitHub remote | Public publication can leak files that are not meant for release |
| release pipeline → release assets | Packaging may accidentally include blocked binaries or internal-only content |
| public docs → end user | Broken or incomplete instructions can cause unsafe/manual workarounds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | I | public repo contents | mitigate | Exclude `.sisyphus/`, keep blocked artifact checks, and verify tracked files before push |
| T-quick-02 | T | release assets | mitigate | Use `scripts/prepare_release.ps1` plus `scripts/verify_release_artifacts.py` so blocked patterns fail the build |
| T-quick-03 | R | release workflow expectations | mitigate | Document `release.yml` as tag-only GitHub publishing automation and keep tests asserting `v*` trigger |
| T-quick-04 | D | install path | mitigate | Add `docs/INSTALL.md` and test required README/install/release links so users do not hit dead-end setup steps |
</threat_model>

<verification>
- `python -m pytest tests/test_release_docs.py -q`
- `python -m pytest tests/test_release_workflow.py -q`
- `powershell -ExecutionPolicy Bypass -File scripts/prepare_release.ps1 -Version 0.1.0`
- `gh repo view --json url,defaultBranchRef`
- `git ls-remote --heads origin`
</verification>

<success_criteria>
- Public docs cover install, first run, release build, and publish steps without placeholder URLs.
- `.sisyphus/` is confirmed unnecessary for runtime/release and excluded from the public repo path.
- `.github/workflows/release.yml` is either retained as documented tag-only release automation or removed only if replaced by an equally explicit manual publish flow.
- A portable zip, SBOM, and checksums are generated locally and the repo is pushed to GitHub.
</success_criteria>

<output>
Quick plan only — no phase summary file required.
</output>
