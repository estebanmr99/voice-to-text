# Runtime dependencies — pinned for reproducible installs
PySide6==6.8.0
sounddevice==0.5.5
numpy==1.26.4
pywhispercpp==1.4.1
pywin32==306
pynput==1.8.1
httpx==0.28.1

# WebRTC VAD requires Visual C++ Build Tools on Windows.
# Uncomment after installing Build Tools from:
# https://visualstudio.microsoft.com/visual-cpp-build-tools/
# webrtcvad-wheels==2.0.11

# faster-whisper and ctranslate2 are dev-only; do not ship
# Install with: pip install faster-whisper ctranslate2
